<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.log">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">>
        <activity
            android:name=".InfoActivity"
            android:label="Info"/>
        <activity
            android:name=".NotificacionActivity"
            android:label="Notificacion"/>
        <activity
            android:name=".SettingsActivity"
            android:label="Settings"/>
        <activity
            android:name=".ComunicateActivity"
            android:label="Comunicate"/>
        <activity
            android:name=".AprendeActivity"
            android:label="Aprende"/>
        <activity
            android:name=".RastreaActivity"
            android:label="Rastrea"/>
        <activity
            android:name=".AlmacenesActivity"
            android:label="Almacenes"/>
        <activity
            android:name=".RegistroActivity"
            android:label="Registro"/>
        <activity
            android:name=".HomeActivity"
            android:label="Home"/>
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
